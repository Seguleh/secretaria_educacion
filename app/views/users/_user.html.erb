<li>
  <%= link_to user.nombre+" "+user.apellido, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete, data: { confirm: "Estas seguro?" } %>
  <% end %>
</li>